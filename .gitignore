node_modules/
dist/
# The mock build the end-to-end suite serves, and what Playwright writes: the
# HTML report, traces and screenshots of a failed run.
dist-e2e/
playwright-report/
test-results/
coverage/
cdk.out/
*.tsbuildinfo
# Scratch space for anything a local run produces: translated files, reports,
# uploads held by the local development API. Never next to the corpus, never
# in a commit.
.local/
.env
.env.*
!.env.example
.DS_Store
