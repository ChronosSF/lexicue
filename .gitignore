node_modules/
dist/
coverage/
cdk.out/
*.tsbuildinfo
# Scratch space for anything a local run produces: translated files, reports,
# uploads held by the local development API. Never next to the corpus, never
# in a commit.
.local/
.env
.env.*
!.env.example
.DS_Store
