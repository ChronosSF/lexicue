# Copy this file to .env and paste the key after the equals sign. The copy is
# git-ignored. Only the command-line tool and the eval runner read it, and a
# value here wins over the shell, so this checkout always uses its own key.
ANTHROPIC_API_KEY=
