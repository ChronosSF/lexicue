/** Product limits from spec section 3.2. */
export const MAX_CUES_PER_FILE = 10_000;
export const MIN_CUES_PER_FILE = 1;
export const MAX_FILE_BYTES = 5 * 1024 * 1024;
export const MAX_FILES_PER_UPLOAD = 50;
export const MAX_UPLOAD_BYTES = 25 * 1024 * 1024;

/** Default frame rate assumed for MicroDVD files that carry no frame-rate line. */
export const DEFAULT_FRAME_RATE = 23.976;
